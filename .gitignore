# Build
/build/
/out/
/dist/
*.o
*.a
*.so
compile_commands.json

# vcpkg
/vcpkg_installed/

# Secrets — NEVER commit exchange keys
.env
.env.*
secrets.*
*.key
config/*.local.*

# Data — never commit collected market data
/data/
*.parquet
*.zst
*.bin

# Editor / OS
.idea/
.DS_Store

# Research
*/__pycache__/
*.pkl